# Backend REST + WebSocket base URLs (browser-visible — use localhost for local dev)
NEXT_PUBLIC_API_URL=http://localhost:8090
NEXT_PUBLIC_WS_URL=ws://localhost:8090

# LiveKit server URL (browser connects directly via WebRTC)
NEXT_PUBLIC_LIVEKIT_URL=ws://localhost:7880
